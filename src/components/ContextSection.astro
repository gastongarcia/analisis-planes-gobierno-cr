---
import { analysisData } from '../data/analysis';
const { education, security } = analysisData.context;
---
<section id="contexto" class="py-20 px-6">
  <div class="max-w-7xl mx-auto">
    <div class="mb-16 text-center">
      <h2 class="text-4xl md:text-5xl font-display text-slate-900 mb-4">EL CONTEXTO ACTUAL</h2>
      <p class="text-slate-600 max-w-2xl mx-auto">
        Antes de ver las propuestas, es crucial entender el punto de partida. Costa Rica enfrenta una crisis estructural en dos frentes críticos.
      </p>
    </div>

    <div class="grid md:grid-cols-2 gap-8">
      {/* Educación */}
      <div class="bg-white p-8 rounded-3xl border border-slate-100 shadow-xl shadow-slate-200/50 hover:shadow-2xl transition-all duration-500 group">
        <div class="w-14 h-14 bg-primary/10 rounded-2xl flex items-center justify-center text-primary mb-6 group-hover:scale-110 transition-transform">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/></svg>
        </div>
        <h3 class="text-2xl font-bold text-slate-900 mb-2">{education.title}</h3>
        <p class="text-primary font-medium mb-6">{education.subtitle}</p>
        
        <div class="space-y-6">
          <div>
            <h4 class="text-xs font-bold text-slate-400 uppercase tracking-widest mb-3">Estado Actual</h4>
            <ul class="space-y-4">
              {education.state.map(item => (
                <li class="flex gap-3 text-slate-600 text-sm leading-relaxed">
                  <span class="text-primary shrink-0 font-bold">•</span>
                  <span set:html={item.replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')}></span>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 class="text-xs font-bold text-slate-400 uppercase tracking-widest mb-3">Causas Principales</h4>
            <ul class="space-y-4">
              {education.causes.map(item => (
                <li class="flex gap-3 text-slate-600 text-sm leading-relaxed">
                  <span class="text-primary shrink-0 font-bold">•</span>
                  <span set:html={item.replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')}></span>
                </li>
              ))}
            </ul>
          </div>
          
          <div class="p-6 bg-slate-50 rounded-2xl border border-slate-100 italic text-slate-700 text-sm leading-relaxed">
            <h4 class="text-[10px] font-bold text-slate-400 uppercase tracking-widest mb-2 not-italic">¿Dónde estamos hoy?</h4>
            "{education.currentStatus}"
          </div>
        </div>
      </div>

      {/* Seguridad */}
      <div class="bg-white p-8 rounded-3xl border border-slate-100 shadow-xl shadow-slate-200/50 hover:shadow-2xl transition-all duration-500 group">
        <div class="w-14 h-14 bg-secondary/10 rounded-2xl flex items-center justify-center text-secondary mb-6 group-hover:scale-110 transition-transform">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
        </div>
        <h3 class="text-2xl font-bold text-slate-900 mb-2">{security.title}</h3>
        <p class="text-secondary font-medium mb-6">{security.subtitle}</p>
        
        <div class="space-y-6">
          <div>
            <h4 class="text-xs font-bold text-slate-400 uppercase tracking-widest mb-3">Estado Actual</h4>
            <ul class="space-y-4">
              {security.state.map(item => (
                <li class="flex gap-3 text-slate-600 text-sm leading-relaxed">
                  <span class="text-secondary shrink-0 font-bold">•</span>
                  <span set:html={item.replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')}></span>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 class="text-xs font-bold text-slate-400 uppercase tracking-widest mb-3">Causas Principales</h4>
            <ul class="space-y-4">
              {security.causes.map(item => (
                <li class="flex gap-3 text-slate-600 text-sm leading-relaxed">
                  <span class="text-secondary shrink-0 font-bold">•</span>
                  <span set:html={item.replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')}></span>
                </li>
              ))}
            </ul>
          </div>
          
          <div class="p-6 bg-slate-50 rounded-2xl border border-slate-100 italic text-slate-700 text-sm leading-relaxed">
            <h4 class="text-[10px] font-bold text-slate-400 uppercase tracking-widest mb-2 not-italic">¿Dónde estamos hoy?</h4>
            "{security.currentStatus}"
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
